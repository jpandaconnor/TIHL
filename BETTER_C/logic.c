#include <stdio.h>

int main(int argc, char *argv[]) {
    7-6; // This is perfectly valid in C...lol

    /**
     * Simples, +, -, *, /, %, ++, --  (All the same things really)
     */

    /**
     *  &&, ||, !  (Logical operators, same things)
     */

    /**
     *  =, +=, -=, *= (Assignment ops....woop)
     */

    /**
     * /= - Divde and assign
     * %= - Modulus and assign
     */

    /**
     * ==, !=, <, >, >=, <= (Same things again)
     */

    /**
     * BITWISE + SHIFT OPERATORS, LET'S DO IT
     * 
     * Operates in bits on integer values
     * Not used in common programs
     * 
     * Can use a single integer to store several bits of information
     */

}